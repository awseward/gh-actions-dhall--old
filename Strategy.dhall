{ Type =
    { fail-fast : Optional Bool
    , max-parallel : Optional Natural
    , matrix : Optional {}
    }
, default =
    { fail-fast = None Bool, max-parallel = None Natural, matrix = None {} }
}
